{
  "name": "zentask-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm install && NODE_ENV=production npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}
